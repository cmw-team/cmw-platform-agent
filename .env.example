HUGGINGFACE_API_KEY=your_huggingface_api_key_here
GEMINI_KEY=your_gemini_key_here
GROQ_API_KEY=your_groq_api_key_here
TAVILY_API_KEY=your_tavily_api_key_here
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Fetch OpenRouter pricing at startup (true|false)
OPENROUTER_FETCH_PRICING_AT_STARTUP=true
MISTRAL_API_KEY=your_mistral_api_key_here
# Polza.ai (OpenAI-compatible Russian LLM gateway, billing in RUB)
POLZA_API_KEY=your_polza_api_key_here
POLZA_BASE_URL=https://polza.ai/api/v1
# Optional: RUB per 1 USD exchange rate for cost display (e.g. 90 means 90 ₽ = $1)
POLZA_RUB_TO_USD_RATE=85
# Required GigaChat params
GIGACHAT_API_KEY=your_gigachat_api_key_here
GIGACHAT_SCOPE=your_gigachat_scope_here
GIGACHAT_VERIFY_SSL=false
# Optional
GIGACHAT_BASE_URL=https://gigachat.sberdevices.ru/v1
GIGACHAT_TIMEOUT=30
# Also drives VL adapter selection: AGENT_PROVIDER=polza routes VL calls through Polza
AGENT_PROVIDER=openrouter
# Default model (if not set, uses first model for selected provider)
# Updated 2026-04-23: Qwen 3.6 Plus (78.8% SWE-bench, $1.30/M)
AGENT_DEFAULT_MODEL=qwen/qwen3.6-plus
## Comma-separated allowlist of enabled providers (empty = allow all)
LLM_ALLOWED_PROVIDERS=gemini,groq,huggingface,openai,openrouter,polza,mistral,gigachat
# History compression
HISTORY_COMPRESSION_ENABLED=true
# Vision-Language routing
VL_DEFAULT_MODEL=qwen/qwen3.6-plus
# Audio: OpenRouter id (e.g. google/gemini-2.5-flash) or gemini-* (resolved per VL_GEMINI_PROVIDER).
VL_AUDIO_MODEL=xiaomi/mimo-v2-omni
# openrouter | google | polza | auto — how bare gemini-* ids are mapped.
VL_GEMINI_PROVIDER=polza
# Optional. If set, overrides VL_GEMINI_PROVIDER only for YouTube videoUrl routing
# (e.g. google to use direct Gemini API; openrouter for OpenRouter). Empty = use VL_GEMINI_PROVIDER.
VL_YOUTUBE_GEMINI_PROVIDER=polza
VL_YOUTUBE_MODEL=gemini-2.5-flash
# Image generation — override the default model used by generate_ai_image.
# Leave empty to use the registry default (google/gemini-3.1-flash-image-preview).
# See agent_ng/image_models.py for all registered models and their available providers.
IMAGE_GEN_PROVIDER=polza   # select provider; must be listed in the model's providers[]
IMAGE_GEN_DEFAULT_MODEL=google/gemini-3.1-flash-image-preview
ENABLE_FALLBACK_MODEL=true
# Updated 2026-04-23: Grok 4.20 (tool calling, 2M context, $4.40/M)
FALLBACK_MODEL_DEFAULT=x-ai/grok-4.20
GRADIO_DEFAULT_LANGUAGE=ru
GRADIO_DEFAULT_PORT=7860
CMW_DEBUG_MODE=false
CMW_VERBOSE_LOGGING=false
# OpenAI-compatible endpoint IO debug logging — writes JSONL file (secrets redacted)
OPENAI_COMPAT_DEBUG_LOG=false
OPENAI_COMPAT_DEBUG_LOG_PATH=openai_compat_io_debug.jsonl
GRADIO_CONCURRENCY_LIMIT=3
GRADIO_MAX_THREADS=100
GRADIO_ENABLE_QUEUE=true
CHAT_CONCURRENCY_LIMIT=3
STATS_CONCURRENCY_LIMIT=3
FILE_CONCURRENCY_LIMIT=3
LOGS_CONCURRENCY_LIMIT=5
ENABLE_CONCURRENT_PROCESSING=true
# Max tool-call iterations per turn (streaming agent)
LANGCHAIN_MAX_TOOL_CALL_ITERATIONS=25

# -----------------------------------------------------------------------------
# External MCP tools (agent_ng/mcp_tools.py; registry: config/mcp_servers.yaml)
# Off by default — when false, native tools only (no HTTP MCP load at startup).
# -----------------------------------------------------------------------------
# Master switch: true loads HTTP/streamable_http servers from the YAML registry
# at startup (preload_mcp_tools / get_cached_mcp_tools) and merges them with
# native tools (duplicate names skip the MCP tool). Default: false.
# CMW_MCP_ENABLED=false
#
# Prefix each MCP tool name with its server key (e.g. comindware_kb__ask_…).
# Passed to MultiServerMCPClient(tool_name_prefix=…). Default: true.
# CMW_MCP_TOOL_NAME_PREFIX=true
#
# Comma-separated allowlist of server keys under `servers:` in mcp_servers.yaml.
# Empty or unset = all entries in the registry. Example matches comindware_kb.
# CMW_MCP_ALLOWED_SERVERS=comindware_kb
#
# Comma-separated host allowlist for MCP URLs (hostname only, lowercased).
# Supports exact hosts and fnmatch patterns (e.g. *.example.com). Empty or unset
# permits any host (logged at INFO — set this in production). Must include the
# host from each server URL or load_mcp_registry raises ValueError.
# CMW_MCP_ALLOWED_HOSTS=ennoia.slickjump.org
#
# Optional cap on how many MCP tools are registered after discovery (first N).
# Empty or invalid = no cap. Does not affect native tools.
# CMW_MCP_MAX_TOOLS=20
#
# Planned HTTP connect/read timeout (seconds). NOT read by agent_ng/mcp_tools.py
# yet — langchain-mcp-adapters client uses library defaults until wired.
# CMW_MCP_CONNECT_TIMEOUT_SEC=30
#
# Gate for live HTTP MCP pytest (agent_ng/_tests/test_mcp_tools_integration.py).
# Set to 1 to run @pytest.mark.integration MCP tests; 0 or unset skips them.
# CMW_MCP_INTEGRATION_TESTS=0
#
# Bearer token for optional Authorization headers in mcp_servers.yaml. Use
# Authorization: "Bearer ${REMOTE_MCP_BEARER_TOKEN}" in headers; ${VAR} expands
# from the environment (missing vars become empty). Keep the token here in .env
# only — never commit real values. Example registry server: comindware_kb.
REMOTE_MCP_BEARER_TOKEN=

# Comindware Platform server connection settings
CMW_BASE_URL=http://your-host/
# Optional dual-host migration (read-only source / write target):
# CMW_BASE_URL_RU=https://{source-host}/
# CMW_BASE_URL_EN=https://{target-host}/
CMW_LOGIN=your_api_user
CMW_PASSWORD=your_api_user_password
# Optional: prefix for new UAT account passwords (test env); final = prefix + CMW_PASSWORD
# UAT_ACCOUNT_PASSWORD_PREFIX=your_uat_prefix
CMW_TIMEOUT=60
CMW_USE_DOTENV=false

# Live integration tests (tools/_tests/test_integration_*_api_live.py): set to 1 to run against the server.
# CMW_INTEGRATION_TESTS=1
# Sandbox application and template *system names* (required when CMW_INTEGRATION_TESTS=1). Do not commit real customer names.
# CMW_INTEGRATION_APP=YourApplicationSystemName
# CMW_INTEGRATION_TEMPLATE=YourTemplateSystemName
# Optional: Document field system name on that template (default: Document).
# CMW_INTEGRATION_DOCUMENT_ATTR=Document
# Optional: Image field to use or create (default set in test code: IntegrationTestImage).
# CMW_INTEGRATION_IMAGE_ATTR=IntegrationTestImage

# Refresh interval for UI components
UI_REFRESH_INTERVAL=10
ITERATION_REFRESH_INTERVAL=10

# Logging config
# Global minimum log level: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=DEBUG
# Text output format preset: simple | verbose
# - simple: time level logger: message
# - verbose: includes filename:line (and optionally process/thread below)
LOG_FORMAT=verbose
# Max length of a single log line on console (truncate beyond)
LOG_CONSOLE_MAX_LENGTH=400
# Emit JSON lines instead of text (true|false)
LOG_JSON=false
# Optional file output (leave empty for console-only)
# Example: logs/app.log
# Logs are created with date suffix: logs/cmw-agent-20260508.log
LOG_FILE=logs/cmw-agent.log
# Max size per log file in bytes before rotation to .1, .2, etc.
LOG_MAX_BYTES=1048576
# Max total size in MB. Oldest files deleted when exceeded.
LOG_MAX_SIZE_MB=100
# Propogate messages to ancestor loggers (true|false)
LOG_PROPAGATE=true
# Include process id in verbose format (true|false)
LOG_INCLUDE_PROCESS=false
# Include thread name in verbose format (true|false)
LOG_INCLUDE_THREAD=false
# Force reconfigure logging at startup (true|false)
# Use when hot-reloading or changing handlers dynamically
LOG_FORCE=false

# Enable OpenAI-compatible endpoint debug log (true|false, default off)
# Stored in LOG_FILE's directory as openai_compat_io_debug.jsonl
# OPENAI_COMPAT_DEBUG_LOG=true

# Bypass proxy for localhost/127.0.0.1 (comma-separated)
# Helps when local proxy causes 503 errors on startup
NO_PROXY=localhost,127.0.0.1

# Timezone for datetime tool and context injection (IANA timezone name)
# Examples: UTC, Europe/Moscow, America/New_York, Asia/Dubai
DEFAULT_TIMEZONE=Europe/Moscow  # branch: kept Europe/Moscow (main had UTC)

# Skill runtime (activates .agents/skills/<name>/SKILL.md)
CMW_SKILLS_DIR=.agents/skills
CMW_SKILLS_ENABLED=true
CMW_SKILL_MAX_BODY_CHARS=60000
