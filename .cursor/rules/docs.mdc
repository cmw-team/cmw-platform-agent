---
description: Rules for writing docs
globs: .md
alwaysApply: true
---

When generating reports about changes, readmes, explanations:

1. Place the .md files to @/docs folder
2. Follow GitHub Markdown format
3. Add prefix to filename: `YYYYMMDD_`. Make sure to generate the timestamps using python now() or bash or powershell. do not make them up